package Project;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.naming.directory.SearchResult;
import javax.swing.*;


	//This part of the program is created only to test different bits of code that is gathered from the internt
	// and used in order to try and test different pieces of them and implement them into the actual real code. 
	//This is not a part of the real code that will be running this project.
	
	import java.sql.*;  
	public class Test{  
	public static void main(String args[]){  
	
		/*
		 * This part of the project was to test code from the internet. It serves no purpose to the final result of
		 * project.
		 */
		
		
		
		
		
		
		
		/*try{  

	Connection con=DriverManager.getConnection(  
	"jdbc:mysql://localhost:3306/Test","root","Tdd&08047728");  
	//here sonoo is database name, root is username and password  
	Statement stmt=con.createStatement();  
	ResultSet rs=stmt.executeQuery("select * from emp");  
	while(rs.next())  
	System.out.println(rs.getInt(1)+"  "+rs.getString(2)+"  "+rs.getString(3)); 
	String Sql = "insert into emp" + "(id, name, ade)" + "values ('11548', 'Micky Frank', '45552')";
	stmt.executeUpdate(Sql);
	System.out.println("Inserted data.");
	con.close();  
	}catch(Exception e){ System.out.println(e);}  
	
	}  */
	}  }
	
	
	/*public static void main(String[] args) {
	
		      JButton btn = new JButton("Demo Button");
		      ActionListener actionListener = new ActionListener() {
		         public void actionPerformed(ActionEvent event) {
		            String str = event.getActionCommand();
		            System.out.println("Clicked = " + str);
		         }
		      };
		      btn.setActionCommand("FirstButton");
		      btn.addActionListener(actionListener);
		      JOptionPane.showMessageDialog(null, btn);
		   }
		
	   }*/
	
/*
  public static void main(String args[]){
	       JFrame frame = new JFrame("My First GUI");
	       frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	       frame.setSize(300,300);
	       JButton button = new JButton("Press");
	       frame.getContentPane().add(button); // Adds Button to content pane of frame
	       frame.setVisible(true);
	    }
	    
	    
	}
 */

